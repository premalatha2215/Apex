        //limit
        List<Account> acc=[SELECT id, Name FROM Account  WITH USER_MODE Limit 5 ];
        for(Account a:acc  ) { System.debug('Name'+ a.Name);}
        //Insert contact relaed to a Account
        Account acc1=[SELECT id,name from Account where Industry='Banking'  WITH USER_MODE Limit 1];
        Contact con =new Contact();
        con.FirstName='Harish';
        con.LastName='P';
        con.AccountId=acc1.Id;
        con.Phone='345677654';
        System.debug('Contact is inserted');
        insert as user con;
        //retrieve all the fields from account obj
        SELECT id, Na